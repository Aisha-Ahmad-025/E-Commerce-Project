// import express from 'express'
// import protect from '../middleware/authMiddleware.js'
// import admin from '../middleware/adminMiddleware.js'
// import {createOrder, getOrder, getOrderById, updateOrderStatus} from '../controller/orderController.js'

// const router = express.Router()


// router.route('/').get(protect, admin, getOrder).post(protect, createOrder)

// router.route('/:id').get(protect, getOrderById).put(protect, updateOrderStatus)
